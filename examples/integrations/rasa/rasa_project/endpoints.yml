model_groups:
  - id: openai_llm
    models:
      - provider: openai
        model: gpt-4o
        api_key: ${OPENAI_API_KEY}

  - id: openai_embeddings
    models:
      - provider: openai
        model: text-embedding-3-large
        api_key: ${OPENAI_API_KEY}

action_endpoint:
  actions_module: actions
